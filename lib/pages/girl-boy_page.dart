import 'package:flutter/material.dart';

class GirlBoyPage extends StatelessWidget {
  const GirlBoyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      // Title: Child
      // Girl and Boy buttons
      // Choose one text
      // Cat image (centered below buttons)
    );
  }
}

